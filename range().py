#--------------------------------------------------------------------------------
# we use a range() function to return a sequence of numbers, starting from 0 by default, 
# and increment by 1 (by default), and stop before a specified number.
#--------------------------------------------------------------------------------


for number in range(4, 8, 2):
    print(number)